import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 18,
    flexDirection: 'row',
    justifyContent: 'space-between',
    width: '100%',
  },
});
