import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    paddingTop: 80,
    flex: 1,
    alignItems: 'center',
  },
  label: {
    marginTop: 32,
    textAlign: 'center',
    color: 'white',
    fontWeight: '500',
    marginHorizontal: 40,
  },
});
